{
  "name": "jwtgentableau",
  "description": "Sample Backend with websockets for triggering data source refresh in frontend initiated via back end, Tableau JS API v3",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Alex Eskinasy",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "ws": "^8.14.2"
  }
}
